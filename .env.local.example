# Supabase (optional). If both are omitted, Bylaw runs in Local Demo mode using localStorage.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solana cluster the app targets for execution + explorer links.
# devnet | testnet | mainnet-beta
NEXT_PUBLIC_SOLANA_NETWORK=testnet
